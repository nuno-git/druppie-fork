# Upload size limit — enforce at the reverse proxy level so oversized
# payloads are rejected before reaching the Python backend.
# Match the backend's MAX_FILE_SIZE (50 MB) with headroom for multipart overhead.
client_max_body_size 55m;
