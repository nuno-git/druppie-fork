{{- if .Values.cnpg.enabled }}
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: {{ include "druppie.fullname" . }}-gitea-db
  labels:
    {{- include "druppie.labels" . | nindent 4 }}
    app.kubernetes.io/component: gitea-db
spec:
  instances: {{ .Values.cnpg.gitea.instances | default 3 }}
  postgresql:
    parameters:
      shared_buffers: "128MB"
      max_connections: "100"
  storage:
    size: {{ .Values.cnpg.gitea.storage | default "5Gi" }}
    {{- if .Values.persistence.storageClass }}
    storageClass: {{ .Values.persistence.storageClass | quote }}
    {{- end }}
  monitoring:
    enablePodMonitor: {{ .Values.cnpg.monitoring.enabled | default false }}
{{- end }}
