eval:
  name: planner-two-step-plan
  description: "Planner should create a 2-step plan (working agent + planner)"
  tags: [planner, plan-structure, tier:standard]
  assertions:
    - agent: planner
      tool_called: builtin:make_plan
  judge:
    checks:
      - "The plan should have exactly 2 steps: one working agent and one planner re-evaluation step"
