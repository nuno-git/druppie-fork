test:
  name: edge-empty-message
  mode: live
  description: "Router handles empty or very short message"
  sessions: []
  run:
    message: "hi"
    real_agents: [router]
  hitl: non-technical-pm
  judge: default
  evals:
    - eval: agent-uses-done
  evaluate:
    judge:
      checks:
        - "The router should handle the short message gracefully without crashing"
