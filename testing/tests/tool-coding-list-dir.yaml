## REPLAY TEST: Full workflow replay — router creates project, architect lists files.
## Everything happens for real: Gitea repo created, files listed.

test:
  name: tool-coding-list-dir
  mode: replay
  description: "Replay: router creates project, architect lists directory"

  sessions: []

  seed_sessions:
    - session: tool-test-list-dir
      mode: replay

  run:
    message: ""
    real_agents: []

  evals:
    - eval: router-correct-intent
      expected:
        intent: create_project

  evaluate:
    assertions:
      - agent: architect
        tool_called: coding:list_dir
        result_valid:
          - not_empty
          - no_error
