test:
  name: planner-create-project-initial
  mode: live
  description: "Planner plans BA as first step for create_project"
  sessions: []
  run:
    message: "build me a recipe sharing app"
    real_agents: [router, planner]
  hitl: non-technical-pm
  judge: default
  evals:
    - eval: router-correct-intent
      expected:
        intent: create_project
    - eval: planner-correct-next-agent
    - eval: planner-two-step-plan
