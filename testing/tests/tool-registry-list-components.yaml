## REPLAY TEST: registry:list_components returns component data
## Executes a list_components tool call through the real MCP registry server.

test:
  name: tool-registry-list-components
  mode: replay
  description: "registry:list_components returns component data"

  sessions: []

  seed_sessions:
    - session: tool-test-registry
      mode: replay

  run:
    message: ""
    real_agents: []

  evaluate:
    assertions:
      - agent: architect
        tool_called: registry:list_components
        result_valid:
          - not_empty
