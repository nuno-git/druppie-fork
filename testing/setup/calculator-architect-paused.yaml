## Project paused waiting for architect approval.
## Shows: HITL question from BA, approval flow, paused state.

metadata:
  id: calculator-architect-paused
  title: "create a scientific calculator"
  status: paused_approval
  user: admin
  intent: create_project
  project_name: calculator
  hours_ago: 3

agents:
  - id: router
    status: completed
    tool_calls:
      - tool: builtin:set_intent
        arguments:
          intent: create_project
          project_name: calculator
          description: "A scientific calculator with expression parsing"
        status: completed
        result: "Intent set to create_project"
      - tool: builtin:done
        arguments:
          summary: "Classified as create_project for calculator"
        status: completed

  - id: business_analyst
    status: completed
    tool_calls:
      - tool: coding:make_design
        arguments:
          content: "# Functional Design: Calculator\n## Requirements\n- Expression parsing\n- Scientific functions (sin, cos, log)\n- History of calculations"
        status: completed
        result: "Created docs/functional-design.md"
      - tool: builtin:done
        arguments:
          summary: "DESIGN_APPROVED"
        status: completed

  - id: architect
    status: running
    planned_prompt: "Design the calculator architecture with expression parser."

messages:
  - role: user
    content: "create a scientific calculator"
