## Shared base: todo-app pipeline up to builder planning
## Router → Planner → BA → Architect → Builder Planner → Test Builder
## Both create-todo-app and create-todo-app-mock-deploy extend this.

tool-test:
  name: setup-todo-app-pipeline
  description: "Shared pipeline prefix for todo-app e2e tests"
  tags: [setup, create-project]

  chain:
    # --- Router ---
    - agent: router
      tool: builtin:set_intent
      arguments:
        intent: create_project
        project_name: todo-app
        description: "A simple to-do list application"
      assert:
        result:
          - not_empty
          - { contains: "todo-app" }

    - agent: router
      tool: builtin:done
      arguments:
        summary: "Classified as create_project for todo-app"
      assert:
        completed: true

    # --- Planner (initial) ---
    - agent: planner
      tool: builtin:make_plan
      arguments:
        steps:
          - agent_id: business_analyst
            prompt: "Gather requirements for the to-do app and create functional design"
          - agent_id: planner
            prompt: "Re-evaluate after business_analyst completes"

    - agent: planner
      tool: builtin:done
      arguments:
        summary: "Planned business_analyst as next step"

    # --- Business Analyst ---
    - agent: business_analyst
      tool: builtin:hitl_ask_multiple_choice_question
      arguments:
        question: "What task management features do you need?"
        choices:
          - "Basic: add, complete, delete tasks"
          - "Advanced: priorities, due dates, categories"
          - "Full: subtasks, recurring tasks, collaboration"
      mock: true
      mock_result: "Basic: add, complete, delete tasks"

    - agent: business_analyst
      tool: coding:make_design
      approval:
        status: approved
        by: analyst
      arguments:
        path: docs/functional-design.md
        content: |
          # Functioneel Ontwerp: Todo App

          ## Vereisten
          - FR-01: Gebruiker kan taken toevoegen met een titel
          - FR-02: Gebruiker kan taken als voltooid markeren
          - FR-03: Gebruiker kan taken verwijderen
          - FR-04: Takenlijst wordt persistent opgeslagen

          ## Gebruikersinterface
          - Invoerveld bovenaan voor nieuwe taken
          - Lijst met taken, elk met checkbox en verwijderknop
          - Filter: alle / actief / voltooid
      assert:
        result:
          - not_empty
          - { contains: "functional-design.md" }

    - agent: business_analyst
      tool: coding:run_git
      arguments:
        command: "add docs/functional-design.md"

    - agent: business_analyst
      tool: coding:run_git
      arguments:
        command: "commit -m 'Functioneel ontwerp toegevoegd'"

    - agent: business_analyst
      tool: coding:run_git
      arguments:
        command: "push"

    - agent: business_analyst
      tool: builtin:done
      arguments:
        summary: "DESIGN_APPROVED"

    # --- Planner (after BA) ---
    - agent: planner
      tool: builtin:make_plan
      arguments:
        steps:
          - agent_id: architect
            prompt: "Review functional design and create technical design for the todo app"
          - agent_id: planner
            prompt: "Re-evaluate after architect completes"

    - agent: planner
      tool: builtin:done
      arguments:
        summary: "BA approved, planned architect as next step"

    # --- Architect ---
    - agent: architect
      tool: coding:run_git
      arguments:
        command: "pull"

    - agent: architect
      tool: coding:read_file
      arguments:
        path: docs/functional-design.md
      assert:
        result:
          - not_empty
          - { contains: "Functioneel Ontwerp" }

    - agent: architect
      tool: coding:make_design
      approval:
        status: approved
        by: architect
      arguments:
        path: docs/technical-design.md
        content: |
          # Technisch Ontwerp: Todo App

          ## Stack
          - Frontend: React met Vite
          - State: localStorage voor persistentie
          - Styling: CSS modules

          ## Componenten
          - App: hoofdcomponent, beheert state
          - TodoInput: invoerveld voor nieuwe taken
          - TodoList: lijst van TodoItem componenten
          - TodoItem: enkele taak met checkbox en verwijderknop
          - FilterBar: alle / actief / voltooid

          ## Datamodel
          ```
          interface Todo {
            id: string
            title: string
            completed: boolean
            createdAt: string
          }
          ```
      assert:
        result:
          - { contains: "technical-design.md" }

    - agent: architect
      tool: coding:run_git
      arguments:
        command: "add docs/technical-design.md"

    - agent: architect
      tool: coding:run_git
      arguments:
        command: "commit -m 'Technisch ontwerp toegevoegd'"

    - agent: architect
      tool: coding:run_git
      arguments:
        command: "push"

    - agent: architect
      tool: builtin:done
      arguments:
        summary: "DESIGN_APPROVED"

    # --- Planner (after architect → builder_planner) ---
    - agent: planner
      tool: builtin:make_plan
      arguments:
        steps:
          - agent_id: builder_planner
            prompt: "Create a builder plan for the todo app"
          - agent_id: planner
            prompt: "Re-evaluate after builder_planner completes"

    - agent: planner
      tool: builtin:done
      arguments:
        summary: "Architect approved, planned builder_planner"

    # --- Builder Planner ---
    - agent: builder_planner
      tool: coding:run_git
      arguments:
        command: "pull"

    - agent: builder_planner
      tool: coding:read_file
      arguments:
        path: docs/functional-design.md

    - agent: builder_planner
      tool: coding:read_file
      arguments:
        path: docs/technical-design.md

    - agent: builder_planner
      tool: coding:write_file
      arguments:
        path: docs/builder-plan.md
        content: |
          # Builder Plan: Todo App

          ## Solution Strategy
          React SPA with localStorage persistence.

          ## Code Standards
          - ESM imports, functional components
          - CSS modules for styling

          ## Test Framework
          Vitest + React Testing Library

          ## Files to Create
          - src/App.jsx (main component)
          - src/components/TodoInput.jsx
          - src/components/TodoList.jsx
          - src/components/TodoItem.jsx
          - src/components/FilterBar.jsx
          - src/index.css

    - agent: builder_planner
      tool: coding:run_git
      arguments:
        command: "add docs/builder-plan.md"

    - agent: builder_planner
      tool: coding:run_git
      arguments:
        command: "commit -m 'Add builder plan'"

    - agent: builder_planner
      tool: coding:run_git
      arguments:
        command: "push"

    - agent: builder_planner
      tool: builtin:done
      arguments:
        summary: "Agent builder_planner: Created builder-plan.md with solution strategy, code standards, and file list."

    # --- Planner (after builder_planner → test_builder) ---
    - agent: planner
      tool: builtin:make_plan
      arguments:
        steps:
          - agent_id: test_builder
            prompt: "Write tests for the todo app based on the builder plan"
          - agent_id: planner
            prompt: "Re-evaluate after test_builder completes"

    - agent: planner
      tool: builtin:done
      arguments:
        summary: "Planned test_builder"

    # --- Test Builder ---
    - agent: test_builder
      tool: coding:run_git
      arguments:
        command: "pull"

    - agent: test_builder
      tool: coding:read_file
      arguments:
        path: docs/builder-plan.md

    - agent: test_builder
      tool: coding:get_test_framework
      mock: true
      mock_result: '{"framework": "vitest", "config_file": "vitest.config.js"}'

    - agent: test_builder
      tool: coding:write_file
      arguments:
        path: src/__tests__/App.test.jsx
        content: |
          import { describe, it, expect } from 'vitest';

          describe('Todo App', () => {
            it('should add a todo', () => {
              expect(true).toBe(true);
            });

            it('should toggle a todo', () => {
              expect(true).toBe(true);
            });

            it('should delete a todo', () => {
              expect(true).toBe(true);
            });

            it('should filter todos', () => {
              expect(true).toBe(true);
            });
          });

    - agent: test_builder
      tool: coding:run_git
      arguments:
        command: "add src/__tests__/App.test.jsx"

    - agent: test_builder
      tool: coding:run_git
      arguments:
        command: "commit -m 'Add todo app tests (Red phase)'"

    - agent: test_builder
      tool: coding:run_git
      arguments:
        command: "push"

    - agent: test_builder
      tool: builtin:done
      arguments:
        summary: "Agent test_builder: Wrote 4 tests in src/__tests__/App.test.jsx (vitest)."

    # --- Planner (after test_builder → builder) ---
    - agent: planner
      tool: builtin:make_plan
      arguments:
        steps:
          - agent_id: builder
            prompt: "Implement the todo app to pass the tests"
          - agent_id: planner
            prompt: "Re-evaluate after builder completes"

    - agent: planner
      tool: builtin:done
      arguments:
        summary: "Planned builder"
