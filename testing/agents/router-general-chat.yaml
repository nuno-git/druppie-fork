agent-test:
  name: router-general-chat
  description: "Router classifies a question as general_chat (real LLM call)"
  tags: [router]

  message: "what agents do you have?"
  agents: [router]
  hitl: non-technical-pm

  assert:
    - check: router-correct-intent
      expected:
        intent: general_chat
