agent-test:
  name: router-empty-projects
  description: "Router creates project with no existing projects"
  tags: [router]

  message: "build me a task management app"
  agents: [router]
  hitl: non-technical-pm

  assert:
    - check: router-correct-intent
      expected:
        intent: create_project
        project_name: "*"
