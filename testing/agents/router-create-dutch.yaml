agent-test:
  name: router-create-dutch
  description: "Router handles Dutch language request"
  tags: [router, language]

  message: "maak een applicatie voor het beheren van vergaderingen"
  agents: [router]
  hitl: dutch-water-authority

  assert:
    - check: router-correct-intent
      expected:
        intent: create_project
        project_name: "*"
